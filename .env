# Auth token for coordinator MCP server (required when binding beyond loopback)
COORDINATOR_AUTH_TOKEN=

# Coordinator MCP server port (default: 3100)
COORDINATOR_PORT=3100

# Session storage directory (default: ./sessions)
SESSIONS_DIR=./sessions
