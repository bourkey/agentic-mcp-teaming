## 1. Config schema — `tls` block and bind-hardening flag

- [x] 1.1 In [src/config.ts](../../../src/config.ts), add an optional `Tls` Zod object: `{ certFile: z.string(), keyFile: z.string(), caFile: z.string().optional(), requireClientCert: z.boolean().optional() }`. Add `.refine(...)` so `requireClientCert === true` requires `caFile` to be present, with message "tls.caFile is required when tls.requireClientCert is true".
- [x] 1.2 Add `tls: Tls.optional()`, `allowInsecureNonLoopback: z.boolean().optional()`, and `allowedHosts: z.array(z.string()).optional()` (operator-provided DNS-rebinding allowlist) to `McpConfig`.
- [x] 1.3 Do NOT add any field that disables TLS verification — there is intentionally no insecure/skip-verify option. Add a one-line comment recording that this omission is deliberate.
- [x] 1.4 Add an optional `hsts` object to the `Tls` schema: `{ enabled: z.boolean().default(true), maxAge: z.number().int().nonnegative().default(31536000), includeSubDomains: z.boolean().default(false), preload: z.boolean().default(false) }`. Add a `.refine(...)` so `preload === true` requires `includeSubDomains === true` and `maxAge >= 31536000`, message naming the preload prerequisites.

## 2. Server — TLS listener, bind hardening, DNS-rebinding

- [x] 2.1 In [src/server/index.ts](../../../src/server/index.ts), add a `isLoopbackHost(host: string): boolean` helper: true for `localhost`, `127.` prefix, `::1`, and the expanded IPv6 loopback. Export it for testing.
- [x] 2.2 Extend `startHttpServer` options to accept resolved TLS material: `tls?: { cert: Buffer; key: Buffer; ca?: Buffer; requireClientCert?: boolean }`, `allowInsecureNonLoopback?: boolean`, and `dnsRebinding?: { allowedHosts: string[]; allowedOrigins?: string[] }`. Pass them from a single options object rather than widening the positional signature further.
- [x] 2.3 Bind-hardening guard at the top of `startHttpServer`: if `!isLoopbackHost(host)` and `tls === undefined` and `allowInsecureNonLoopback !== true`, throw an `Error` naming the insecure bind (caller exits non-zero). If `!isLoopbackHost(host)` and `tls === undefined` and `allowInsecureNonLoopback === true`, `console.warn` that traffic is unencrypted.
- [x] 2.4 When `tls` is provided, create the listener with `https.createServer({ cert, key, ...(ca ? { ca } : {}), ...(requireClientCert ? { requestCert: true, rejectUnauthorized: true } : {}) }, app)` and `.listen(port, host, ...)`; otherwise keep `app.listen(port, host, ...)`. Return the same close function shape. Update the startup `console.log` to report `https`/`http` and mTLS on/off.
- [x] 2.5 In the `/mcp` initialize branch, when `dnsRebinding` is provided, pass `enableDnsRebindingProtection: true`, `allowedHosts`, and (if set) `allowedOrigins` into the `StreamableHTTPServerTransport` constructor options.
- [x] 2.6 `import * as https from "node:https"` (and `node:tls` types if needed). Do not add the `ca`/`requestCert` keys when not configured (respect `exactOptionalPropertyTypes`).
- [x] 2.7 HSTS: when `tls` is provided and `tls.hsts.enabled !== false`, register an Express middleware (before the routes) that sets `Strict-Transport-Security` to `max-age=<maxAge>` + `; includeSubDomains` (if set) + `; preload` (if set). Register it ONLY on the HTTPS path — never when serving plain HTTP. Build the header string once at startup, not per request.

## 3. Wiring — read cert files, derive defaults, thread through `src/index.ts`

- [x] 3.1 In [src/index.ts](../../../src/index.ts), both the `serve` and `start` paths: when `config.tls` is set, read `certFile`/`keyFile`/`caFile` with `fs.readFileSync` (fail fast with a clear error naming an unreadable path) into Buffers, and build the `tls` options object for `startHttpServer`.
- [x] 3.2 Build `dnsRebinding` only when `config.allowedHosts` is non-empty: `{ allowedHosts: config.allowedHosts }`. Do NOT auto-derive from `host:port` (the bind host is not the client-sent `Host`). Absent ⇒ no `dnsRebinding` (protection off).
- [x] 3.3 Pass `allowInsecureNonLoopback` from config through to `startHttpServer`. Update the existing positional `startHttpServer(makeServer, port, host, authToken)` call sites to the options-object form from task 2.2.

## 4. Tests

- [x] 4.1 `tests/tls-transport.test.ts` — add a helper that generates a throwaway CA + server cert (and a client cert for mTLS) at runtime using `node:crypto` `generateKeyPairSync` + a minimal self-signed X.509 (or a checked-in fixture generated by a `scripts/` helper). The test client MUST **trust the generated CA** (supply it as `ca` to the client transport's `https.Agent`/`fetch`, or via `NODE_EXTRA_CA_CERTS`). Never set `rejectUnauthorized: false`.
- [x] 4.2 `tests/tls-transport.test.ts` — TLS happy path: coordinator started with a `tls` block serves `https`; a `StreamableHTTPClientTransport` over `https://…/mcp` trusting the test CA completes `register_session`.
- [x] 4.3 `tests/tls-transport.test.ts` — mTLS accept: `requireClientCert: true`; a client presenting a CA-signed client cert registers successfully.
- [x] 4.4 `tests/tls-transport.test.ts` — mTLS reject: `requireClientCert: true`; a client with no client cert fails the TLS handshake (assert the connection/handshake error; no HTTP response).
- [x] 4.5 `tests/tls-bind-hardening.test.ts` — non-loopback host + no TLS + no opt-out: `startHttpServer` (or the serve binary) refuses to start / exits non-zero with the insecure-bind error.
- [x] 4.6 `tests/tls-bind-hardening.test.ts` — non-loopback + `allowInsecureNonLoopback: true`: starts over plain HTTP and emits the unencrypted-traffic warning; loopback + no TLS: starts with no warning (unchanged).
- [x] 4.7 `tests/tls-transport.test.ts` — config validation: `requireClientCert: true` without `caFile` fails `loadConfig`/schema parse with the documented message.
- [x] 4.8 `tests/tls-transport.test.ts` — HSTS: a response over `https` (TLS configured) includes `Strict-Transport-Security: max-age=31536000`; a response over plain HTTP includes no such header; with `tls.hsts.enabled: false` the header is absent over https; config with `preload: true` but no `includeSubDomains` fails validation.
- [x] 4.9 Reuse `tests/serve-harness.ts`; extend `startCoordinator` to accept a `tls`/cert-paths option and an env passthrough for `NODE_EXTRA_CA_CERTS`. Run `npm test` — existing SSE/streamable/coexistence tests stay green over plain HTTP.

## 5. Documentation

- [x] 5.1 `README.md` — extend the "Transports" / deployment section: `tls` config example (including an `hsts` example), mTLS example, `https://` `.mcp.json` snippets, the bind-hardening behaviour, the HSTS header (and the `.dev`-preload context + `includeSubDomains`/`preload` caveat), and a note that clients must trust the server CA (`NODE_EXTRA_CA_CERTS`).
- [x] 5.2 `docs/peer-bus-runbook.md` — cross-machine deployment section: provisioning server cert/key (`chmod 600` the key), CA + per-machine client certs for mTLS, the fail-closed guard and its `allowInsecureNonLoopback` opt-out, and the TLS-terminating reverse-proxy alternative (with ACME) as a documented option.

## 6. Verification

- [x] 6.1 `npm run build && npm test` — all green, no regression in existing transport tests.
- [x] 6.2 `npm run lint` on changed `src/` files introduces no new errors over the pre-existing baseline.
- [ ] 6.3 Manual smoke (operator, cross-machine or two-host loopback alias): start with a `tls` block, point a `type: "http"` client at `https://<host>:<port>/mcp` with the CA trusted, confirm `register_session` over TLS; then with `requireClientCert: true` confirm a client without a cert is refused.
